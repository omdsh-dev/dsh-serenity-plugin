# Slice C — Wire / Persistence / Client Contracts (dsp vs DSH 0.1.2-rc.1)

> Reviewer: slice-C agent (read-only). Scope: every dsp dependency on a host-owned wire string,
> envelope field, RPC name, persistence rule, or client slot contract.
> dsp = `AI_LAB/dsh-serenity-plugin/hooks/dsh-serenity-hooks` (v1.30.5).
> Host = `AI_LAB/dsh-harness-public` @ 0.1.2-rc.1 (the stated production host).
> No source file was modified; this report is the only write.

## 1. Verdict summary (counts per area)

| # | Area | Verified OK | P0 | P1 | P2 |
|---|---|---|---|---|---|
| 1 | Session persistence & event-append contract | 8 | 1 (C-1) | 1 (C-4) | 1 (C-8) |
| 2 | Host RPC / wire surface (gateway, auth, proxy) | 6 | 1 (C-2) | 1 (C-3) | 2 (C-5, C-6) |
| 3 | Settings API (`installSection`) | 3 | 0 | 0 | 0 |
| 4 | Client plugin contract | 10 | 0 | 0 | 1 (C-7) |
| 5 | Hard-coded host literals sweep (§4) | — | 1 (C-1) | 1 (C-2) | 2 (C-5, C-6) |
| | **Total** | **27** | **2** | **2** | **4** |

The motivating bug (`attachment-error` → `session/attachment-invalid`) is **fixed** in dsp v1.30.5 and
the fix matches rc.1 source (V-23). Two unrelated contracts of the same class are still broken: the
session-log event vocabulary (C-1) and the external workspace-creation lockdown (C-2).

## 2. Verified OK

| ID | Contract | dsp site | host site |
|---|---|---|---|
| V-1 | `Session.append(type, data)` two-arg form for a non-surface type is legal (no `surfaceOp` required, none allowed) | `src/session-bound.ts:113-121`; `src/rebuild.ts:340-344` | `packages/core/session/src/index.ts:668-698`; `packages/core/session/src/surface.ts:33-38,195-212` |
| V-2 | `compaction/prune` payload fields `shadowedRange/shadowedSeqs/shadowedTokenCount`, log-only | `src/rebuild.ts:340-344` | `packages/compaction/compaction/src/types.ts:82-89`; `packages/llm/token-meter/src/surface-projection.ts:66-80` |
| V-3 | Surface `replace` requires `sourceEventSeqs` covering **every** shadowed node, referencing earlier seqs only | `src/rebuild.ts:347-354` | `packages/core/session/src/surface.ts:246-269`; `packages/core/session/src/types.ts:411-427` |
| V-4 | `user/message` must carry `source` (`{kind:'user'}`) or later metadata reads throw | `src/rebuild.ts:349-350` | `packages/llm/llm/src/message.ts:103,131-145`; `packages/core/session/src/index.ts:308-324` |
| V-5 | Event reading must use `snapshotEvents()` (rc.1 removed `.events`) | `src/session-ops.ts:52-64` (all consumers route here) | `packages/core/session/src/index.ts:600`; `packages/extensions/tool-cordis/src/api-catalog.ts:4799` |
| V-6 | `deriveEventMessage` / `tokenMeter.estimateMessage` used for shadow pricing | `src/rebuild.ts:42,337-338,414` | `packages/core/session/src/surface.ts:90`; `packages/llm/token-meter/src/index.ts:194-199` |
| V-7 | Session log disk layout `<root>/<project>/<encoded-id>/session.jsonl[.zstd]`, root = `dshHomePath('sessions')` | `src/session-cleanup.ts:22-25,49-86` | `packages/bundle/base/cordis.patch.yml:110-113`; `packages/session/session-persistence-jsonl/src/format.ts:37,180-223`; `.../src/index.ts:48,481-536` |
| V-8 | Live-session protection via `ctx.sessions.list()` shape (`header.cwd`) | `src/api.ts:168-172,736-739`; `src/weixin-bridge.ts:398` | `packages/core/session/src/index.ts:1119-1129` |
| V-9 | BrowserAuth cookie name `dsh-auth-<sha256(authority)>`, minted through `writeHead(303, {'set-cookie': …})`, authority-bound to the `Host` header | `src/gateway-dsh-auth.ts:30-37,61-83` | `packages/client/connection/src/browser-auth.ts:16,106-123,240-266,289-302` |
| V-10 | In-process cookie exchange through `ctx.connection.authenticatedUrl()` + `authorizeIndex()` | `src/gateway-dsh-auth.ts:22-27,61-83`; `src/gateway.ts:560-592` | `packages/client/connection/src/rpc-host.ts:102-108`; `packages/bundle/web-app/src/index.ts:271-284` |
| V-11 | Trust fence: `Host` must be loopback/trusted and `Origin.host === Host.host` → dsp rewrites both to `127.0.0.1:<mainPort>` | `src/gateway-proxy.ts:138-149`; `src/gateway.ts:167-174,444-463` | `packages/client/connection/src/api-request-trust.ts:91-114`; `packages/client/connection/src/rpc-host.ts:97` |
| V-12 | Generic WebSocket upgrade proxying still carries the stream mux `/api/remote.mux` | `src/gateway.ts:436-512` | `packages/api/gateway/src/stream-protocol.ts:5-6`; `packages/api/gateway/src/index.ts:205-228` |
| V-13 | `server-response` envelope `{type,rpcId,result:{ok,error:{code,message,details}}}` | `src/gateway-proxy.ts:77-83`; `src/gateway.ts:286-293,306-313` | `packages/client/connection/src/rpc.ts:60-76`; `packages/client/connection/src/client/rpc.ts:73-102` |
| V-14 | Client `prompt()` resolves `RemoteResult` with `error.code/message/details` | `src/client/image-fallback-api.ts:103-107` | `packages/api/session-controller/src/client/contract/session.ts:83-88`; `packages/typert/protocol/src/types.ts:74-76`; `.../src/remote-error.ts:22-31` |
| V-15 | `ctx.sessions.binding(id)?.session.prompt(...)` is the supported client entry | `src/client/image-fallback-api.ts:103-106` | `packages/api/session-controller/src/client/contract/sessions.ts:122`; `.../src/client/sessions/service.ts:128-131,509` |
| V-16 | `settings.installSection(owner, ns, schema, entry, hooks)` signature + owner argument | `src/settings-section.ts:164-179` | `packages/settings/settings/src/index.ts:472-496` |
| V-17 | `SettingsSectionHooks = {setSource, onChange, validate?}` | `src/settings-section.ts:143-163` | `packages/settings/settings/src/index.ts:871-891` |
| V-18 | Settings namespace must be a lowercase hyphenated identifier; `settings` service declared in `inject` | `src/settings-section.ts:40`; `src/index.ts:52` | `packages/settings/settings/src/index.ts:470,479-482` |
| V-19 | Slots `conversation.input.dock`, `conversation.session.header.actions`, `settings.section` exist with the used scopes | `src/client/index.ts:50,61,82,102` | `packages/client/ui-conversation/src/client/contract/slots.ts:105,127`; `packages/client/ui-settings/src/client/contract/slots.ts:54` |
| V-20 | `slots.register({name,id,order,label,inject}, Component)`; `SlotLabel = string \| (() => string)` | `src/client/index.ts:49-109` | `packages/client/ui-slots/src/index.ts:505,590-637,772-799` |
| V-21 | `InputZone = {session, input}` + session-standard `inputActions {setDraft, removeImage, submit}` | `src/client/ImageFallbackDock.tsx:48-63` | `packages/client/ui-conversation/src/client/contract/slots.ts:155-162,197-200`; `.../contract/input.ts:221-231` |
| V-22 | `InputState = {draft, imageIds}` (image ids are `DraftAttachmentId`) | `src/client/ImageFallbackDock.tsx:43-46,68`; `src/client/FileFallbackDock.tsx:30-32` | `packages/client/ui-conversation/src/client/contract/input.ts:321-325,166` |
| V-23 | **Motivating bug fixed**: rejection code is `session/attachment-invalid` (main) / `subagent/attachment-invalid` (child), reason `MODEL_DOES_NOT_SUPPORT_IMAGES`, surfaced as `snapshot.promptError.error.{code,details.reason}` | `src/client/image-fallback-api.ts:31-34`; `src/client/ImageFallbackDock.tsx:32-40,67-73` | `packages/api/session-controller/src/commands.ts:316-324`; `.../src/types.ts:194`; `packages/subagent/subagent/src/control.ts:111-117`; `.../src/client/contract/snapshot.ts:58-62,81`; `.../src/client/sessions/session.ts:262` |
| V-24 | `ClientContext = Context` from `@deepseek-ai/cordis`; `SettingsScope`/`SettingsScopeSpec` re-exported from `.../ui-settings/client` | `src/client/image-fallback-api.ts:12-13`; `src/client/index.ts:25-26,37-40` | `packages/client/ui-settings/src/client/index.ts:32`; `.../src/client/settings-contract.ts:37-46` |
| V-25 | `ctx.settingsScope.bind(spec)` + `getSnapshot/subscribe/set/unset/mutate` | `src/client/index.ts:94-97`; `src/client/SettingsSection.tsx:192-206` | `packages/client/ui-settings/src/client/settings-scope.ts:254,282`; `.../src/client/settings-contract.ts:54-88` |
| V-26 | `ctx.get('conversation').draftImages(ids) → [{file}]` (method call, not destructured) | `src/client/image-fallback-api.ts:85-95` | `packages/client/ui-conversation/src/client/apply.ts:323`; `.../src/client/service.ts:162` |
| V-27 | Non-image paste is still rejected by the host (`intakeImages` → `createDraftImages` throws), so dsp's capture-phase `preventDefault` premise holds | `src/client/FileFallbackDock.tsx:53-65`; `src/client/file-fallback-api.ts:19-28` | `packages/client/ui-conversation/src/client/apply.ts:307-317`; `.../src/client/service.ts:134-141,255,411` |
| V-28 | Host lifecycle event names dsp listens to all exist (`agent/session-start`, `agent/pre-step`, `agent/turn-stopping`, `agent/status`, `agent/inbox/inserted`, `system-prompt/assemble`, `tools/pre-execute`, `tools/post-execute`, `session/event`, `session/created`) | `src/seams/context.ts:219,230`; `src/seams/keeper.ts:170`; `src/seams/guards.ts:349`; `src/seams/bootstrap.ts:261,277,332,382`; `src/rebuild.ts:395`; `src/output-guard-seam.ts:56`; `src/tools/handyman.ts:67`; `src/autopilot-trajectory.ts:386`; `src/weixin-bridge.ts:412` | `packages/extensions/tool-cordis/src/api-catalog.ts:2934,2942,2966,2974,2982,3182,3270,3302,3310`; `packages/core/scope/src/scoped-events.generated.ts:16-34` |
| V-29 | Service names dsp resolves all exist (`tools`, `webServer`, `sessions`, `shellEnv`, `skills`, `agentLoop`, `agents`, `systemPrompt`, `sessionProjections`, `settings`, `sessionTitle`, `tokenMeter`, `workspaceRegistry`, `sessionPersistence`, `codeRuntime`, `agentPresets`, `connection`, `conversation`, `slots`, `settingsScope`) | `src/index.ts:52`; `src/rebuild.ts:372,414`; `src/api.ts:281,335`; `src/skiff-debug.ts:89,99`; `src/seams/keeper.ts:110`; `src/handyman-preset-inherit.ts:43,51`; `src/client/index.ts:34` | `super(ctx, '<name>')`: `packages/core/tools/src/index.ts:820`; `packages/host/webserver/src/index.ts:144`; `packages/core/session/src/index.ts` (Sessions); `packages/shell/shell-env/src/index.ts:100`; `packages/skill/skill/src/index.ts:376`; `packages/core/agent-loop/src/index.ts:377`; `packages/core/agent/src/index.ts:260`; `packages/core/system-prompt/src/index.ts:405`; `packages/session/session-projection/src/index.ts:208`; `packages/session/session-title/src/index.ts:311`; `packages/llm/token-meter/src/index.ts:105`; `packages/workspace/workspace/src/index.ts:115`; `packages/session/session-persistence/src/index.ts:124`; `packages/code-runtime/code-runtime/src/index.ts:123`; `packages/preset/agent-presets/src/index.ts:164`; `packages/client/ui-conversation/src/client/service.ts:162`; `packages/client/ui-settings/src/client/settings-scope.ts:254` |

## 3. Findings

| ID | Severity | Contract | Evidence (dsp / host) | Failure mode | Fix |
|---|---|---|---|---|---|
| **C-1** | **P0** | Session-log event vocabulary: a stored event type unknown to the build makes the log unreadable | dsp: `src/session-bound.ts:27-48,106-127` (declares + appends `serenity/bound` via `session.append`, **no `ignorable`**); write sites `src/tools/session.ts:399,457,491`, `src/seams/context.ts:193`, `src/skiff-core.ts:131`, `src/rebuild.ts:297`. Host: `packages/core/session/src/index.ts:668-698` (append builds `{type,seq,time,data,surfaceOp?,sourceEventSeqs?}` — **no way to set `ignorable`**); `packages/core/session/src/known-event-types.ts:9-22` ("Every `SessionEventMap` member declared in this repository… Downstream (out-of-repo) plugin events are outside this list by construction"); `packages/session/session-persistence/src/coordinator.ts:1248-1252` (refuse unless `event.ignorable === true`), call sites `:1027,:1057,:1074,:1509`; rationale `.agents/notes/implemented/architecture/2026-08-30-retain-ignorable-external-session-events.md:9-17` (append-time refusal deliberately avoided: "the unknown-type guard is deliberately read-side only" `coordinator.ts:772-776`) | **Loud on next load, silent until then.** Every session where dsp wrote a binding persists an event outside the build's vocabulary; the first cold load (resume, WebUI history open, `sessionPersistence.inspect`) throws `SessionFormatUnsupportedError`. Nothing warns at write time. | Stop putting the binding in a custom event type. Preferred: persist it outside the session log (e.g. `$DSH_HOME/serenity/bindings.json` keyed by session id, or the CCC's `AGENT_SESSIONS/`). If the log must carry it, the envelope has to include `ignorable: true`, which `Session.append` cannot express — that would require writing through `ctx.sessionPersistence.append(id, [event])` and accepting a divergence from the live in-memory log (not recommended), or an upstream host registration mechanism. Runtime confirmation on the installed bundle is **not verifiable from inside the CCC** (path escape blocked); the source-level rule is version-independent. |
| **C-2** | **P0** | Host RPC addressing: endpoint name, HTTP path, and request envelope for `workspace.create` | dsp: `src/gateway.ts:277` (`url.pathname === '/api/workspace.create'`), `:297-310` (`parsed.payload.path`), `src/gateway-proxy.ts:68-74`. Host rc.1: endpoint key is `${namespace}/${method}` (`packages/typert/registry/src/service.ts:63-68`) → `workspace/create`; the client posts to `${channel}/${endpoint}` with envelope `{type:'client-request', rpcId, method, payload}` (`packages/client/connection/src/client/rpc.ts:12-51`) → `POST /api/workspace/create`; payload is `{args:{…}}` (`packages/api/gateway/src/client/index.ts:443,468`); the gateway claims exactly two-segment endpoints (`packages/api/gateway/src/index.ts:266-269`) | **Silent.** The branch never matches, so the proxy forwards the call: with `allowWorkspaceCreate=false` external users can still create workspaces, and the whitelist is not enforced. Even if the path matched, `payload.path` is now `payload.args.path` → `undefined` → whitelist-empty passes, whitelist-non-empty denies everything. No log line either way. | Match `POST /api/workspace/create`; parse `{rpcId, method, payload:{args:{path}}}`; keep a legacy branch only if 0.1.1 support is still required. Add a unit test built from the rc.1 envelope (`method` field, `args` wrapper). |
| **C-3** | **P1** | Workspace whitelist visibility enforcement | dsp: `src/gateway.ts:231-233` (list-filter branch removed), `src/gateway-proxy.ts:38-62` (`filterWorkspaceList`, now uncalled), `src/config-ops.ts:431-433`, setting `gateway.workspaces`. Host: rc.1 has no `workspace.list` unary — the client uses `remote.workspace.follow(signal)` (`packages/api/workspace-controller/src/client/index.ts:81`) delivered as a WebSocket stream on `/api/remote.mux` (`packages/api/gateway/src/stream-protocol.ts:5-6`; `packages/api/gateway/src/index.ts:205-228`) | **Silent gap.** The setting now gates only `create`; an external (3081) user sees every registered workspace, which the config name implies is not the case. | Either filter `upsert` frames on the proxied mux stream (per-connection frame rewrite), or drop `gateway.workspaces` from the UI/docs and state explicitly that only creation is gated. |
| **C-4** | **P1** | `$DSH_HOME` resolution and the sessions root | dsp: `src/session-cleanup.ts:22-25`, `src/config-ops.ts:136-141` (`process.env.DSH_HOME ?? join(process.env.HOME ?? '', '.dsh')`). Host: `packages/util/home-paths/src/index.ts:12-18,79-88` — precedence is *explicit configured path > `$DSH_HOME` > `~/.dsh`*, and a **blank/whitespace `$DSH_HOME` is treated as unset** (asserted `packages/util/home-paths/tests/home-paths.spec.ts:43-45`); the sessions root is `dshHomePath('sessions')` (`packages/bundle/base/cordis.patch.yml:110-113`) | **Silent wrong path.** With `DSH_HOME=""` dsp derives relative `sessions/` and `serenity-hooks.json` (CWD-relative) instead of `~/.dsh/...`; with a configured home (`path:` in the profile patch) dsp reads/writes the wrong location. Session cleanup could then scan an unrelated tree. | Use `resolveDshHome`/`dshHomePath` from `@deepseek-ai/dsh-home-paths` (add the peer dep) or replicate blank-as-unset; better, read the configured root from the persistence service instead of deriving it. |
| **C-5** | P2 | Dead workspace-list filter + stale host-path comments | dsp: `src/gateway.ts:52,64` (imported, never called), `src/gateway-proxy.ts:38-62`, doc `src/gateway-proxy.ts:39-40` (`POST /api/workspace.list`), `src/gateway.ts:183`. Host: no `workspace.list` endpoint in rc.1 (grep of `packages/` finds only `workspace/create` and `workspace/follow`) | **Silent.** `filterWorkspaceList` keeps a green unit suite (`tests/gateway.test.ts:347-367`) asserting behavior with no production call site — false assurance that list filtering still exists. | Delete the function, its tests, and the two stale comments; the remaining enforcement surface is C-2. |
| **C-6** | P2 | RPC error codes emitted by dsp's own gateway | dsp: `src/gateway-proxy.ts:81` (`code:'forbidden'`), `src/gateway.ts:293,313` (`code:'bad-request'`). Host: `RemoteErrorCode = keyof RemoteErrorDetailsMap` (`packages/typert/protocol/src/types.ts:56-57`), whose members are namespaced (`gateway/bad-request`, `session/…`, `workspace/…`); the browser parser only checks `typeof error.code === 'string'` (`packages/client/connection/src/client/rpc.ts:88-101`), so these codes are accepted today | **Silent.** Accepted by the lenient parser; any consumer that narrows on the declared code union (or a stricter parser) will not recognize them. | Emit declared codes (`gateway/bad-request`; for the whitelist denial reuse an existing declared code) and assert the emitted string against a pinned list in the gateway test. |
| **C-7** | P2 | Client contract hygiene around the image fallback | dsp: `src/client/ImageFallbackDock.tsx:5` (comment still names `attachment-error` as the trigger), `src/client/image-fallback-api.ts:13` (unused type import `DraftAttachmentId` from the **package root**), `:31-34` (retains retired `attachment-error`). Host: the root entry does not export it — `packages/client/ui-conversation/src/index.ts:1-23` exports only submission settings; the type lives in `packages/client/ui-conversation/src/client/contract/input.ts:166` and is reachable as `@deepseek-ai/dsh-client-ui-conversation/client`. dsp resolves it only through the tsconfig path override `hooks/dsh-serenity-hooks/tsconfig.json:36` | **Silent.** Unused + type-only: no runtime effect; but the file documents a trigger string that no longer exists and imports a name the real package root cannot supply. | Fix the comment to name all three codes; delete the unused import (or import from `.../client`); keep the retired-code branch but label it with the host version that retired it. |
| **C-8** | P2 | One reader for `snapshotEvents()` | dsp: canonical `src/session-ops.ts:52-64`; two private duplicates `src/skiff-core.ts:385-388`, `src/tools/handyman.ts:77-78`. Host: `packages/core/session/src/index.ts:600` (`snapshotEvents(fromSeq, toSeqExclusive)`) | **Silent.** Three copies of the rc.1 compatibility fallback; a future signature change must be applied three times. | Route both modules through the shared helper (they already import from `session-ops.js` elsewhere). |

## 4. Hard-coded host literals inventory

| Literal | dsp sites | Still valid in rc.1? | Break mode | Recommendation |
|---|---|---|---|---|
| `serenity/bound` (custom session event type) | `src/session-bound.ts:33,62,114`; 6 write sites | **No** — outside `KNOWN_SESSION_EVENT_TYPES` | **Silent at write, loud at load** (C-1) | Remove from the log; persist elsewhere (C-1) |
| `/api/workspace.create` (HTTP path) | `src/gateway.ts:277` | **No** — path is `/api/workspace/create` | **Silent** (C-2) | Repoint + test (C-2) |
| `payload.path` (create request field) | `src/gateway.ts:301-303` | **No** — now `payload.args.path` | **Silent** (C-2) | Repoint (C-2) |
| `workspace.list` (HTTP path, docs) | `src/gateway-proxy.ts:39-40`; `src/gateway.ts:183,187,231` | **No** — replaced by `workspace.follow` stream | **Silent** (dead code, C-5) | Delete (C-5) |
| `type:'server-response'`, `rpcId`, `result.ok/error.{code,message,details}` | `src/gateway-proxy.ts:78-82`; `src/gateway.ts:287-293,313` | Yes | Loud only if the client parser tightened | Keep; align codes (C-6) |
| `code:'forbidden'` / `code:'bad-request'` | `src/gateway-proxy.ts:81`; `src/gateway.ts:293,313` | Names not declared in `RemoteErrorDetailsMap` | Silent (lenient parser) | Use declared codes (C-6) |
| `dsh-auth-` cookie prefix | `src/gateway-dsh-auth.ts:34` | Yes | Silent (cookie never found → upstream 401) | Keep; add a test pinning the prefix |
| `?token=` query parameter | `src/gateway-dsh-auth.ts:67` | Yes (`TOKEN_QUERY = 'token'`) | Silent (exchange returns undefined) | Keep |
| `__DSH_BOOT__` | not used by dsp | n/a | — | No action |
| `/api/remote.mux` (WS mux) | not named by dsp (generic upgrade proxy) | Yes (path exists) | Silent if the mux path is renamed | Optional: pin the path in a test that proxies an upgrade |
| `session/attachment-invalid`, `subagent/attachment-invalid`, `attachment-error`, `MODEL_DOES_NOT_SUPPORT_IMAGES` | `src/client/image-fallback-api.ts:32-33` | First two yes; `attachment-error` retired (pre-0.1.2) | Silent (fallback never fires) — **was the motivating bug; now fixed** | Keep all three + comment (C-7) |
| `session/title`, `user/message`, `assistant/message`, `tool/call`, `tool/result`, `compaction/prune` | `src/seams/context.ts:39`; `src/autopilot-trajectory.ts:467`; `src/seams/bootstrap.ts:77,202-205`; `src/skiff-core.ts:396-424`; `src/output-guard-seam.ts:42`; `src/tools/handyman.ts:81`; `src/rebuild.ts:340,347` | Yes (all in `known-event-types.ts:22-74`) | Silent if renamed (string comparisons) | Acceptable; a rename would be a host format change |
| Host lifecycle event names (§V-28) | 11 files | Yes | Silent (cordis accepts unknown event names) | Optional: a single test asserting each name is present in the host catalog |
| `$DSH_HOME` env var + `sessions/` root | `src/session-cleanup.ts:23`; `src/config-ops.ts:140` | Variable yes; derivation diverges | Silent wrong path (C-4) | Use the host resolver (C-4) |
| `x-serenity-ui` header | `src/client/image-fallback-api.ts:68`; `file-fallback-api.ts:60`; `src/api.ts:219` | dsp-owned, not a host literal | — | No action |
| Service names (`sessionTitle`, `tokenMeter`, `workspaceRegistry`, `sessionPersistence`, `codeRuntime`, `agentPresets`, `connection`, `webServer`, `settingsScope`, …) | §V-29 | Yes | Silent (`ctx.get` returns undefined → guard paths) | Optional: assert presence at startup for the ones whose absence degrades silently |

## 5. Method & limits

- **Read (dsp)**: `src/rebuild.ts`, `src/session-bound.ts`, `src/session-ops.ts` (helper + layout), `src/session-cleanup.ts`, `src/gateway.ts`, `src/gateway-proxy.ts`, `src/gateway-dsh-auth.ts`, `src/settings-section.ts`, `src/api.ts` (host-facing parts), `src/client/index.ts`, `ImageFallbackDock.tsx`, `FileFallbackDock.tsx`, `image-fallback-api.ts`, `file-fallback-api.ts`, `tsconfig.json` paths, plus repo-wide greps for host literals, `ctx.on(...)`, `ctx.get(...)`, event types, and service names.
- **Read (host)**: `packages/core/session/src/{index,types,surface,known-event-types}.ts`; `packages/session/session-persistence/src/coordinator.ts`; `packages/session/session-persistence-jsonl/src/{index,format}.ts`; `packages/compaction/compaction/src/types.ts`; `packages/llm/token-meter/src/{index,surface-projection}.ts`; `packages/llm/llm/src/message.ts`; `packages/api/session-controller/src/{commands.ts,types.ts,client/contract/*,client/sessions/*}`; `packages/typert/{protocol,registry}/src/*`; `packages/client/connection/src/{rpc,rpc-host,browser-auth,api-request-trust,client/rpc}.ts`; `packages/api/gateway/src/{index,stream-protocol,client/index}.ts`; `packages/api/workspace-controller/src/{client/index,feed,commands}.ts`; `packages/settings/settings/src/index.ts`; `packages/host/webserver/src/index.ts`; `packages/client/ui-conversation/src/client/{contract/*,apply,service}.ts(x)`; `packages/client/ui-settings/src/client/*`; `packages/client/ui-slots/src/index.ts`; `packages/util/home-paths/src/index.ts`; `packages/bundle/base/cordis.patch.yml`; `packages/extensions/tool-cordis/src/api-catalog.ts` (generated signatures); the `ignorable` Agent Note.
- **Limits / not verified here**:
  1. Runtime behaviour of the **installed** `@deepseek-ai/dsh` bundle (v0.1.2-rc.1) could not be inspected — `/home/yh/.npm-global/...` is outside the CCC root and path escape is blocked. C-1 is therefore argued from the source that the task states is the exact production host; a runtime reproduction (resume a session that has a binding) is the decisive confirmation and should be run by the parent.
  2. No host process was started, no dsp test suite was run (read-only slice; bash disabled).
  3. Client-bundle build/typecheck was not run: C-7's "resolves only via tsconfig paths" is a source-reading conclusion.
  4. Deep semantic verification of hook payload shapes (`agent/pre-step`, `tools/pre-execute/post-execute`) and of the `/serenity/*` routes is out of this slice (event-name existence only).
  5. Slice G already owns the declaration-layer issues (`engines.dsh`, peer deps, `dsh.client.inject`); not repeated here.

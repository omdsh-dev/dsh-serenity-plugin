# 沙箱内安装：store 固定在工作区内（不进 git，见 .gitignore）
store-dir=.pnpm-store
# 自家工程 registry 归官方 npm（D51，2026-09-10）：用户级 ~/.npmrc 的
# nexus.going-link.com 是公司镜像（choerodon 账号）——发布要先显式 registry（D8），
# 安装又会撞 Nexus packument 缓存滞后（1.31.8 实测 ERR_PNPM_NO_MATCHING_VERSION）。
# 本仓（含 hooks 子目录）从此直连官方源，不再受公司镜像影响。
registry=https://registry.npmjs.org/
